
/* the switch statement
 * branches according to *constant* integer values
 *
 * the default case is optional.  if it's absent and
 * no expressions match, then no action takes place
 */
switch (<expression>) {
	case <const-expr>: statements
	case <const-expr>: statements
	default: statements
}
